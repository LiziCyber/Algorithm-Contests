#include<iostream>
#include<algorithm>
using namespace std;
bool is[1005][1005];
int d[1005];
int n;
char c[1005]

int main(){
	cin>>n;
	while(n--){
		memset(is,0,sizeof(is));
		memset(d,0,sizeof(d));
		memset(c,0,sizeof(c));

		cin>>c;

	}
}
